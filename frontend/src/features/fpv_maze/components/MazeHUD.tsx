import React from 'react';

const MazeHUD: React.FC = () => {
  return (
    <div className="maze-hud" style={{ marginBottom: '1rem', color: '#00FF00', fontFamily: 'monospace' }}>
      {/* Placeholder for HUD elements like score, health, current district etc. */}
      <p>MAZE HUD (Placeholder)</p>
    </div>
  );
};

export default MazeHUD;